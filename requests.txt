beautifulsoup4==4.9.3
requests==2.25.1
lxml   # 可选，如果需要更快的解析速度，可以使用lxml替代beautifulsoup的html.parser
threading  # 标准库，通常不需要指定版本，但可以注释掉以明确声明
os       # 标准库，通常不需要指定版本，但可以注释掉以明确声明
collections  # 标准库，通常不需要指定版本，但可以注释掉以明确声明
time     # 标准库，通常不需要指定版本，但可以注释掉以明确声明
logging  # 标准库，通常不需要指定版本，但可以注释掉以明确声明
